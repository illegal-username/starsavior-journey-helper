.gradle/
.idea/
local.properties
**/build/

# Release signing material must stay outside Git.
/signing/
/keystore.properties
*.jks
*.keystore
*.p12

# Real databases are external inputs; internal builds stage them only under build/.
/app/src/*/assets/journey_choices.json

# Distributable binaries are published separately from the source repository.
*.apk
*.aab

# Local Python tool bytecode.
__pycache__/
