# 변경 기록

## 2.1.4 · 2026-08-11

- 반투명 HUD와 밝은 배경의 저채도 색을 게이지로 오인해 실제 값 `100`이 낮은 값이나 증감 예정값으로 흔들리던 문제 수정
- 날짜·목표 배너처럼 게이지와 비슷한 상단 색 띠보다 실제 HUD 높이의 유효 후보를 우선하되, 레터박스나 세로 이동 화면에서는 전체 탐색으로 복귀하도록 보강
- 저채도 배경, 상단 배너, 세로 이동 게이지에 대한 합성 회귀 테스트를 추가하고 실제 제보 영상의 353개 프레임에서 모두 `100`으로 판독되는지 검증

## 2.1.3 · 2026-08-11

- `60`, `80`, `100` 고정값 대신 먼저 일치한 이벤트의 DB에서 난이도별 조건 수치를 읽어 난이도를 판정하도록 변경
- 선택지별 OCR 수치와 같은 선택지의 조건을 대응시켜, 서로 다른 선택지에서 같은 숫자를 쓰는 이벤트도 구분
- 모든 난이도에 공통인 비용과 화면의 무관한 숫자는 판정 근거에서 제외하고, 결과가 모호하면 임의로 필터링하지 않도록 보강
- `비치발리볼 대회 예선전`의 `200/300/500` 등 이벤트마다 다른 조건과 임의의 향후 수치에 대한 회귀 테스트 추가

## 2.1.2 · 2026-08-11

- 화면 전체 폭으로 HUD 크기를 추정하지 않고 상단에서 게이지의 실제 색 띠·높이·중립색 꼬리와 양 끝을 직접 탐색하도록 재설계
- 샌드위치 아이콘과 둥근 끝부분의 비율 보정을 검출된 게이지 높이로 계산해 기기 해상도·갤러리 확대·레터박스의 영향을 제거
- 동일한 원본을 서로 다른 확대율로 표시한 4개 화면에서 모두 같은 `33 → 64`를 산출하도록 수정
- 짧은 회복 예정 구간과 서로 길이가 다른 감소 예정 구간을 각각 분리해서 판독하도록 경계 검출 보강
- 초록색 배경이 세로로 이어진 경우에도 직사각형 끝점이 안정된 행만 후보로 사용하고, 완전 충전·방전 값의 끝단 오차 보정
- 화면별 고정 폭 표를 사용하지 않는 동적 HUD 배율·이동 회귀 테스트 추가

## 2.1.1 · 2026-08-10

- 저장된 이전 위치나 화면 속 초록색 배경보다 실제 게이지의 상·하 테두리 구조를 우선해 HUD가 이동해도 올바른 게이지를 다시 탐색
- 레터박스·화면 공유 크기 차이로 게이지의 세로 위치가 달라져도 실제로 찾은 행을 분석하도록 수정
- 화면 확대·재보간 시 샌드위치 아이콘 주변을 부분 게이지로 오인해 스태미나가 `0` 또는 `100`으로 표시되던 문제 수정
- 신뢰도가 낮은 극단값 후보를 구조 기반 후보로 재검증하고 실제 색 구간까지 게이지 시작점을 보정
- 스태미나 탐지 회귀 테스트를 추가하고 DB 업데이트 요청의 앱 버전 표기를 빌드 버전과 자동으로 일치시킴

## 2.1.0 · 2026-08-09

- 선택지 요구 수치 `60`, `80`, `100`으로 난이도를 판정하고 감지된 난이도의 조건·효과만 표시
- 결과창의 별도 난이도 문구를 제거하고, 인식에 실패한 경우에는 기존처럼 전체 결과를 표시
- 화면 상단 스태미나 게이지를 해상도·화면비에 고정되지 않은 동적 탐색 방식으로 인식
- 현재 스태미나와 훈련·휴식 후 증가·감소 예상값 표시
- 이전 DB 캐시에 난이도 구분 정보가 없으므로 DB 스키마를 4로 갱신해 자동 무효화

## 2.0.1 · 2026-08-07

- 잠재력 보상의 `min/max` 단계값을 10배한 할인율로 표시해 `10% 할인`과 `20% 할인`을 구분
- 할인 보상에도 잠재력 이름과 실제 효과 설명을 함께 유지
- 할인 단계가 없는 잠재력 획득 보상은 기존 표시를 유지
- DB 스키마를 3으로 올려 2.0.0에서 잘못 가공해 저장한 기존 다운로드 캐시를 자동 무시
- 앱 내 수동 DB 변환기와 운영용 내장 DB 생성기를 동일하게 수정하고 회귀 테스트 추가

## 2.0.0 · 2026-08-05

- 공개판 앱 ID를 `helper.journey.starsavior`, 앱 이름을 `스세 여정 도우미`로 확정
- 이벤트명과 선택지를 함께 인식해 동일한 선택지 문구를 가진 이벤트를 구분
- 화면 공유 종료 후에도 플로팅 아이콘을 유지하고 화면 공유 재시작 지원
- 결과창 유지·토글, 길게 누르기 메뉴, DB 업데이트 및 도우미 종료 기능 추가
- 플로팅 아이콘의 원 배경 크기 조절과 그리기·정렬 문제 수정
- 실제 여정 JSON을 공개 저장소에서 제외하고 가상 예제 DB로 공개 빌드·테스트 가능하도록 변경
- 개인정보, 데이터 권리, 보안 및 외부 구성요소 고지 추가
- GitHub Actions에서 실제 DB와 운영 키 없이 단위 테스트·Lint·debug 빌드를 검사하도록 구성
- HTTPS 이외의 평문 네트워크 사용 차단
