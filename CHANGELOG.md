# 변경 기록

## 2.0.0 · 2026-08-05

- 공개판 앱 ID를 `helper.journey.starsavior`, 앱 이름을 `스세 여정 도우미`로 확정
- 기존 시험판과 분리되는 운영 서명 체계로 전환하고 프로젝트에 포함됐던 개발 키와 비밀번호 제거
- 운영 서명정보를 Git에서 제외되는 `keystore.properties` 또는 환경변수에서만 읽도록 변경
- 서명 설정이 없는 release 빌드는 명확한 오류와 함께 중단하도록 검증 작업 추가
- 실제 여정 JSON을 공개 저장소에서 제외하고 가상 예제 DB로 공개 빌드·테스트 가능하도록 변경
- 실제 DB는 로컬 생성 또는 복사 후 운영 APK에만 내장하는 절차 추가
- 개인정보, 데이터 권리, 보안, 기여, 운영 배포 문서와 공개 소스 비밀정보 검사 추가
- GitHub Actions에서 실제 DB와 운영 키 없이 단위 테스트·Lint·debug 빌드를 검사하도록 구성
- HTTPS 이외의 평문 네트워크 사용 차단

## 1.1.0 · 2026-08-03

- 화면 왼쪽의 이벤트명 전용 OCR 영역을 추가하고 오른쪽 선택지 OCR과 함께 매칭
- 이벤트명과 선택지 문구를 결합한 DB 스키마 2로 변경해 동일 선택지의 서로 다른 이벤트를 별도 레코드로 유지
- 선택지를 공유하지만 제목이 다른 이벤트를 이벤트명 OCR로 구분
- 제목이 없거나 불완전해 동일 선택지 후보를 구분할 수 없으면 잘못된 결과 대신 재인식 안내 표시
- 영역 인식이 약할 때 이벤트명과 선택지를 포함한 전체 화면 OCR로 한 번 재시도
- 결과 창에 이벤트명·선택지 인식 점수를 각각 표시
- 내장 DB를 79개 이벤트·187개 선택지로 갱신하고 기존 스키마 1 캐시를 안전하게 무시

## 1.0.5 · 2026-08-03

- 원 배경의 elevation이 흰색 `✦`보다 앞에 그려져 아이콘을 가리던 문제 수정
- 원과 글리프를 하나의 View에서 배경 → 테두리 → 글리프 순서로 직접 그려 Z축 순서가 뒤집히지 않도록 변경
- 폰트 윤곽 경로의 실제 경계를 기준으로 `✦`를 원 중심에 정확히 정렬
- 윤곽에서 가장 먼 점과 안전 여백을 반영해 최소 원 지름을 계산하여 글리프 끝이 원 밖으로 노출되지 않도록 수정

## 1.0.4 · 2026-08-03

- 화면 잠금 등으로 `MediaProjection`이 종료되어도 플로팅 아이콘과 길게 누르기 메뉴를 유지하는 화면 공유 대기 모드 추가
- 대기 전환 시 가상 화면·이미지 리더·진행 중 캡처·OCR 인식기를 해제하고, 유지된 회색 `✦`를 눌러 화면 공유를 다시 허용하는 흐름 추가
- 명시적 종료 전 시스템이 서비스를 다시 만들면 화면 공유 대기 상태의 플로팅 아이콘을 복구하도록 변경
- 가운데 흰색 `✦`와 58dp 터치 영역은 고정하고 보라색 원 배경만 조절하는 슬라이더 및 실시간 미리보기 추가
- 실제 `✦` 글리프 크기를 기준으로 원 배경의 최소 지름을 계산하고, 기존 원 크기를 최대값으로 유지
- 원 배경 설정 저장 및 실행 중인 오버레이에 즉시 반영
- 화면 공유 세션 교체 중 이전 OCR 콜백이 결과창을 띄우지 않도록 세대 검증 추가

## 1.0.3 · 2026-08-03

- 플로팅 `✦` 아이콘을 0.7초 길게 눌러 `DB 업데이트`, `도우미 종료`, `취소` 메뉴 열기
- 메뉴의 종료 동작으로 화면 공유·OCR·오버레이·포그라운드 알림을 함께 정리
- 앱 첫 화면과 오버레이 메뉴에서 사용자가 직접 실행하는 DB 업데이트 추가
- 공개 원자료 JSON 6개를 내려받아 앱 내부 형식으로 변환하고 즉시 OCR 매처에 반영
- ETag 기반 최신 여부 확인, 응답 크기 제한, 스키마·개수·중복·빈 결과 검증, 이전 DB 백업 및 실패 시 기존 DB 유지
- 여정 난이도 라벨에 `[object Object]`가 표시되던 데이터 생성 오류 수정

## 1.0.2 · 2026-08-02

- 결과창 생성 직후 예약된 제거 작업이 새 결과창을 닫던 순서 경쟁 수정
- 결과창을 사용자가 `✦` 또는 `×`를 다시 누를 때까지 유지
- 결과창이 열린 상태의 `✦` 입력은 재인식하지 않고 창만 닫는 토글 동작 추가

## 1.0.1 · 2026-08-02

- Android 15에서 시작 직후 종료되던 `WindowInsetsController` 초기화 순서 수정
- 초기 화면 오류 복구 UI 추가
- 오버레이 설정 Activity가 없는 기기·테스트 이미지용 안전한 대체 안내 추가
- 이후 업데이트를 위한 프로젝트 고정 개인용 서명키 적용

## 1.0.0 · 2026-08-02

- 화면 공유 기반 한 프레임 캡처
- 기기 내 한국어 OCR과 여정 선택지 매칭
- 이동 가능한 오버레이 아이콘과 결과 카드
- 183개 선택지 내장 데이터
