# Copy this file to keystore.properties and keep the copied file out of Git.
# Prefer an absolute path to a keystore stored outside the project directory.
storeFile=/absolute/path/to/starsavior-helper-release.jks
storePassword=
keyAlias=starsavior-helper
keyPassword=
